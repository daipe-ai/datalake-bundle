parameters:
  datalakebundle:
    tables:
      testTable:
        tableName: 'test'
        schemaPath: 'datalakebundle.test.TestSchema'
        targetPath: '/foo/bar/test.delta'

services:
  datalakebundle.test.SparkSessionFactory:

  pyspark.sql.session.SparkSession:
    factory: ['@datalakebundle.test.SparkSessionFactory', 'create']
